sudo: required

language: php

php:
  - 7.1

services:
  - docker

before_install:
  - make build
  - make run
  - docker-compose exec wordpress features/seed-database.sh

script:
  - vendor/bin/behat 
